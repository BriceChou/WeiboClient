<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bg_retweeted_sel"
    android:orientation="vertical">

    <TextView
        android:id="@+id/tv_retweeted_content"
        style="@style/TextViewBody"
        android:layout_width="match_parent"
        android:padding="16dp"
        android:text="这里是引用区域,这里是引用区域这里是引用区域这里是引用区域这里是引用区域这里是引用区域这里是引用区域这里是引用区域这里是引用区域这里是引用区域这里是引用区域这里是引用区域这里是引用区域"
        android:textColor="@color/txt_gray" />

    <include
        android:id="@+id/include_status_image"
        layout="@layout/include_status_image"
        android:visibility="gone" />

</LinearLayout>
